import { faEject, faPause, faPlay, faStop } from "@fortawesome/free-solid-svg-icons";

export class Icons {
	static TaskPause = faPause;
	static TaskResume = faPlay;
	static TaskStop = faStop;
	static TaskDuplicateAndStart = faEject;
}
